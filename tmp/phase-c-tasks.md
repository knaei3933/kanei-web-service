# Phase C + API TODO - 4 tasks

## Role
You are a full-stack developer for a Japanese SMB homepage creation service (Next.js + Tailwind + Vercel).

## Objective
Implement 4 remaining tasks in the admin dashboard and production pipeline.

## Context
- Project: `/mnt/c/Users/kanei/claudecode/02.Homepage_Dev/kanei-web-service/`
- Admin list: `src/app/admin/page.tsx` (735 lines)
- Admin detail: `src/app/admin/[id]/page.tsx` (1663 lines)
- Deploy demo route: `src/app/api/demo/[submissionId]/deployed/route.ts`
- Deliver route: `src/app/api/production/[submissionId]/deliver/route.ts`
- Email lib: `src/lib/demo-feedback-loop.ts` (has email sending patterns)
- Status machine: received -> awaiting_representative_approval -> awaiting_plan_approval -> approved_for_execution -> demo_generating -> demo_revision_ready -> demo_deployed -> customer_approved -> pre_production_interview -> pre_production_review -> production_ready -> delivered
- Admin auth: ADMIN_SECRET env var, Bearer token via Authorization header
- API pattern for admin: `fetch(url, { headers: { Authorization: \`Bearer \${secret}\` } })` where secret comes from sessionStorage or cookie

## Constraints
- Japanese text only (no English labels in UI)
- Maintain existing style patterns (rounded-lg, border, bg-*, text-*)
- No breaking changes to existing API contracts
- tsc --noEmit must pass
- Follow existing email patterns in demo-feedback-loop.ts

## Verification
- `npx tsc --noEmit` passes
- No runtime errors on page load

---

## Task 1: Admin list - direct interview start button (Phase C-1)

**Current** (`admin/page.tsx:170-194`): customer_approved / pre_production_interview / pre_production_review statuses only show a button that navigates to `/admin/[id]` with label "ヒアリング開始 →" etc.

**Problem**: To start an interview, admin must click through to the detail page first.

**Fix**: Make the button in the admin list directly call the API when status is `customer_approved`.

1. In `ActionColumn` component, when status === "customer_approved":
   - Change the button to directly call `POST /api/consult/[id]/interview` with `{ sendMail: true }`
   - Use ADMIN_SECRET from sessionStorage (same pattern as deliver button at line 196-204 which uses `handleDeliver`)
   - Show loading state on the button while fetching
   - On success: refresh submissions list (refetch), show brief success message
   - On failure: show error
   - Button label: "ヒアリング開始" (loading: "送信中...")
   - Use indigo styling (same as current)
   - Keep the "→" detail page link as a secondary small link next to the button

2. When status === "pre_production_review":
   - Same approach: directly call `POST /api/admin/submissions/[id]/pre-production/approve` with `{ action: "approve" }`
   - Button label: "第3ゲート承認" (loading: "処理中...")

3. When status === "pre_production_interview":
   - Keep as navigation button ("回答待ち →") since no action is needed from admin

## Task 2: Interview question customization UI (Phase C-2)

**Current** (`admin/[id]/page.tsx:1459`): There is a placeholder comment `TODO(Phase C): ヒアリング質問をカスタマイズする UI があればここに統合する。`

**Implement**: A simple interview question editor that appears when starting the interview.

1. When status is `customer_approved`, show a "ヒアリング質問を編集" expandable section above the "ヒアリング開始" button
2. Default questions are auto-generated by the API (no need to change the API)
3. The editor shows:
   - A read-only list of default questions (fetched from GET endpoint or hardcoded defaults)
   - A textarea where admin can add custom questions (one per line)
   - These custom questions are sent to the POST /api/consult/[id]/interview endpoint as `customQuestions: string[]`
4. If the interview endpoint does not support customQuestions yet, just show the default questions as read-only info (what the customer will be asked) and skip the editor
5. Default questions to display (read-only info box):
   - "本制作にあたり、優先して取り入れたいページや機能はありますか？"
   - "追加で提供可能な素材（写真・ロゴ・文面など）はありますか？"
   - "競合サイトや参考にしたいサイトで、特に参考にしたい要素は何ですか？"
   - "本サイトの公開に希望する時期や目標はありますか？"
   - "その他、本制作に向けて伝えておきたいことはありますか？"

## Task 3: Demo generation failure recovery route

**Current** (`demo/[id]/deployed/route.ts:160`): TODO comment - no recovery path when demo_generation_failed.

**Implement**:
1. Add a new status `demo_generation_failed` handling in `deployed/route.ts`
2. When demo generation fails (status is already demo_generation_failed and a callback comes in), allow the admin to trigger re-generation
3. In `admin/[id]/page.tsx`: when status is `demo_generation_failed` or when latest demo round failed, show a "デモを再生成" button
4. The button calls `POST /api/admin/submissions/[id]/execute-demo` (this endpoint already exists)
5. Also add `demo_generation_failed` to the status badge colors and labels in `admin/[id]/page.tsx` (similar line 188 pattern)

**Files to modify**:
- `src/app/admin/[id]/page.tsx` - add badge color + label for demo_generation_failed, add re-generation button
- No API changes needed (execute-demo endpoint already exists)

## Task 4: Delivery confirmation email (sendDeliveredEmail)

**Current** (`production/[id]/deliver/route.ts:110`): TODO comment `// TODO: sendDeliveredEmail(submissionId, deliveryInfo);`

**Implement**: A delivery confirmation email function.

1. In `src/lib/demo-feedback-loop.ts` (which has all email patterns), add a new function `sendDeliveredEmail(submissionId: string, deliveryInfo: Record<string, unknown>)`
2. The function should:
   - Read submission.json to get customer info (payload.name, payload.email, payload.companyName)
   - Read delivery-info.json for delivery details (deployUrl, etc.)
   - Use the same email sending pattern as existing functions (sendDemoReadyEmail pattern)
   - Send via the relay mail endpoint (MAIL_RELAY_URL env var + fetch POST)
   - Email to: customer email
   - Subject: "【金井貿易株式会社】ホームページ制作完了のお知らせ"
   - Body in Japanese, including:
     - Greeting with customer name
     - Company name
     - Deploy URL (delivery-info.json's url or deployUrl)
     - How to access the site
     - Support contact info
3. Call this function in deliver route.ts where the TODO comment is
4. Handle email failure gracefully (log error but don't fail the delivery)
